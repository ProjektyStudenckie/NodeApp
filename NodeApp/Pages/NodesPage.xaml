<local:BasePage x:TypeArguments="core:LoginViewModel" x:Class="NodeApp.NodesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NodeApp"
      xmlns:core="clr-namespace:NodeApp.Core;assembly=NodeApp.Core"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="NodePage">

    <Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>



            <!--<local:NodeContentListControl Width="300" VerticalAlignment="Center"/>-->

            <local:NodesListControl/>

            <!--<TextBlock Text="Test"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       FontSize="{StaticResource FontSizeLarge}"
                       Foreground="White"/>-->

            <local:SideMenuControl Width="300" Grid.Column="1"
                                   local:AnimateSlideInFromRightProperty.Value="{
                                        Binding ApplicationViewModel.SideMenuVisible,
                                        Source={x:Static local:ViewModelLocator.Instance}}"/>
        </Grid>
    </Border>
</local:BasePage>
